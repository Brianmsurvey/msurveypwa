import Dashboard from "../containers/Dashboard/Dashboard";

// List of application routes with their corresponding components
var indexRoutes = [{ path: "/", name: "Home", component: Dashboard }];

export default indexRoutes;
